enum Roles {
  ADMIN = "admin",
  MEMBER = "member",
  STAFF = "staff",
  MANAGER = "manager",
}

const userRole: Roles = Roles.ADMIN;

// switch (userRole) {
//   case Roles.ADMIN:
//     break;
//   case Roles.MANAGER:
//     break;
// }
